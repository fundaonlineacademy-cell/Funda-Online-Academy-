(()=>{const wait=setInterval(()=>{if(typeof db==='undefined'||!db||typeof U==='undefined'||!U||typeof CO==='undefined'||!Array.isArray(CO)||!window.dashboard)return;clearInterval(wait);install()},150),n=v=>Number(v||0),sum=(a,f)=>a.reduce((t,x)=>t+n(f(x)),0),approved=x=>String(x.status||x.enrollment_status||'').toLowerCase()==='approved';function values(){const active=CO.filter(x=>x.active!==false),approvedRows=EN.filter(approved);return{active,approvedRows,catalogueValue:sum(active,x=>x.price),enrolmentValue:sum(approvedRows,x=>x.amount||x.course?.price)}}function install(){const originalDashboard=window.dashboard,originalFinance=window.finance;window.dashboard=function(){originalDashboard();const v=values(),box=document.querySelector('#view .metrics');if(box)box.innerHTML=`<div class="metric"><small>Approved Enrolment Finance</small><strong>${money(v.enrolmentValue)}</strong><p>${v.approvedRows.length} approved enrolment(s)</p></div><div class="metric"><small>Active Course Catalogue</small><strong>${v.active.length} courses</strong><p>${money(v.catalogueValue)} total catalogue value</p></div><div class="metric"><small>Students</small><strong>${PR.filter(x=>x.role!=='admin').length}</strong><p>Student profiles</p></div><div class="metric"><small>Pending Approvals</small><strong>${EN.filter(x=>st(x)==='pending').length}</strong><p>Awaiting review</p></div>`;const h=document.querySelector('#view .head p');if(h)h.textContent='Live operations with course catalogue value kept separate from approved-enrolment finance.'};window.finance=function(){originalFinance();const v=values(),box=document.querySelector('#view .metrics');if(box){const extra=document.createElement('div');extra.className='panel financeMeaning';extra.innerHTML=`<h2>How these finance figures work</h2><div class="financeSplit"><div><b>${v.active.length} active courses · ${money(v.catalogueValue)}</b><span>Catalogue value: the combined current price of every active course. Adding a course changes this figure.</span></div><div><b>${v.approvedRows.length} approved enrolments · ${money(v.enrolmentValue)}</b><span>Enrolment value: tuition attached to approved learner enrolments. This changes only when an enrolment is approved or its recorded value changes.</span></div></div>`;box.insertAdjacentElement('afterend',extra)}};const s=document.createElement('style');s.textContent='.metric p{margin:6px 0 0;color:var(--m);font-size:11px}.financeSplit{display:grid;grid-template-columns:1fr 1fr;gap:10px}.financeSplit>div{padding:13px;border:1px solid var(--line);border-radius:10px;background:#f8fbff}.financeSplit b,.financeSplit span{display:block}.financeSplit span{font-size:12px;color:var(--m);line-height:1.5;margin-top:5px}@media(max-width:700px){.financeSplit{grid-template-columns:1fr}}';document.head.appendChild(s);if(typeof current!=='undefined'&&current==='dashboard')window.dashboard()}})();
