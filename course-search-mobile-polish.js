(()=>{
'use strict';
if(!/courses-public\.html$/i.test(location.pathname))return;
const $=id=>document.getElementById(id);
function style(){if($('csmpStyle'))return;const s=document.createElement('style');s.id='csmpStyle';s.textContent=`.csmpWrap{display:grid!important;grid-template-columns:minmax(0,1fr) auto;gap:8px;align-items:stretch}.csmpBtn{border:0;border-radius:12px;padding:0 16px;background:#071d49;color:#fff;font-weight:800;font-size:12px;min-height:50px;cursor:pointer}.csmpStatus{margin-top:8px;font-size:12px;color:#52647a;font-weight:700;min-height:18px}.csmpStatus strong{color:#071d49}@media(max-width:700px){body.csmpSearching #pcvCat,body.csmpSearching #fcsaIntro{display:none!important}.csmpBtn{padding:0 13px}.csmpStatus{font-size:11px}}`;document.head.appendChild(s)}
function mount(){const input=$('courseSearch'),count=$('courseResultCount'),grid=$('courseGrid');if(!input||!count||!grid)return false;if($('csmpSearchBtn'))return true;style();const wrap=input.parentElement;wrap.classList.add('csmpWrap');const btn=document.createElement('button');btn.id='csmpSearchBtn';btn.type='button';btn.className='csmpBtn';btn.textContent='Search';wrap.appendChild(btn);const status=document.createElement('div');status.id='csmpStatus';status.className='csmpStatus';wrap.insertAdjacentElement('afterend',status);
function update(){const q=(input.value||'').trim();document.body.classList.toggle('csmpSearching',!!q);if(!q){status.textContent='';return}const n=Number((count.textContent||'').replace(/[^0-9]/g,''));status.innerHTML=Number.isFinite(n)?`<strong>${n}</strong> ${n===1?'course':'courses'} found for “${q.replace(/[&<>"']/g,'')}”`:`Searching for “${q.replace(/[&<>"']/g,'')}”…`}
function run(jump){input.dispatchEvent(new Event('input',{bubbles:true}));setTimeout(()=>{update();if(jump){input.blur();const target=(Number((count.textContent||'').replace(/[^0-9]/g,''))===0?$('noSearchResults'):grid);target?.scrollIntoView({behavior:'smooth',block:'start'})}},80)}
input.addEventListener('input',()=>setTimeout(update,40));input.addEventListener('search',()=>run(false));input.addEventListener('keydown',e=>{if(e.key==='Enter'){e.preventDefault();run(true)}});btn.addEventListener('click',()=>run(true));new MutationObserver(update).observe(count,{childList:true,characterData:true,subtree:true});update();return true}
let tries=0;function boot(){if(mount())return;if(++tries<80)setTimeout(boot,250)}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();