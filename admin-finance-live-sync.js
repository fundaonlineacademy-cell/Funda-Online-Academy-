(()=>{
'use strict';
if(!/admin-v2\.html$/i.test(location.pathname)||window.__fundaCanonicalFinanceDashboard)return;
window.__fundaCanonicalFinanceDashboard=true;
let db,timer,busy=false;
const n=v=>Number(v||0),money=v=>'R'+n(v).toLocaleString('en-ZA',{maximumFractionDigits:0}),pct=(a,b)=>b?Math.round(n(a)/n(b)*100):0;
function client(){return db||(db=window.supabase?.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY))}
function activeLabel(){return [...document.querySelectorAll('#nav button,.nav button')].find(x=>x.classList.contains('on')||x.classList.contains('active'))?.textContent||''}
function dashboardVisible(){return /dashboard/i.test(activeLabel())}
function financeVisible(){return /finance/i.test(activeLabel())}
async function snapshot(){const c=client();if(!c)return null;const r=await c.rpc('get_admin_executive_snapshot');if(r.error){console.error('Canonical executive snapshot',r.error);return null}return r.data||null}
function patchFinanceCopy(x){const root=document.querySelector('.finx');if(!root)return;const cards=[...root.querySelectorAll('.finCard span')];if(cards[0])cards[0].textContent='Approved tuition · live current course prices';const infos=[...root.querySelectorAll('.finInfo')];const priceInfo=infos.find(el=>/Price integrity:/i.test(el.textContent||''));if(priceInfo)priceInfo.innerHTML=`<b>Live pricing source:</b> current approved tuition is calculated from the live <code>courses.price</code> value for every approved enrolment. Original enrolment amounts are retained separately for audit/history. Current live value: <b>${money(x.approved_tuition)}</b> · original enrolment snapshot: <b>${money(x.enrolment_snapshot_tuition)}</b>.`;const hero=root.querySelector('.finHero p');if(hero)hero.textContent='One live financial source reading current course prices, approved enrolments, verified payments, receivables and the cashbook.'}
function patchExecutive(x){const tuition=n(x.approved_tuition),verified=n(x.verified_payments_all),expenses=n(x.operating_expenses),net=n(x.net_cash_result),margin=tuition?Math.round(net/tuition*100):0,students=n(x.student_profiles),approved=n(x.approved_enrolments),courses=n(x.active_courses),certs=n(x.issued_certificates),completion=pct(certs,approved),verifiedPct=tuition?Math.min(100,pct(verified,tuition)):0;
 const health=[...document.querySelectorAll('.healthGrid .health')];
 if(health[0])health[0].innerHTML=`<h3>📈 Finance Health</h3><strong>${money(tuition)}</strong><small>Approved tuition value · live current course prices</small><div class="bar"><i style="width:${Math.max(0,Math.min(100,margin))}%"></i></div><small class="${net>=0?'good':'warn'}">${money(verified)} verified collections · ${money(expenses)} expenses</small>`;
 if(health[1])health[1].innerHTML=`<h3>👥 Student Growth</h3><strong>${students}</strong><small>Total student profiles · live student role count</small><div class="bar"><i style="width:${Math.min(100,students*4)}%"></i></div><small>${approved} approved enrolment(s)</small>`;
 if(health[2])health[2].innerHTML=`<h3>🎓 Course Performance</h3><strong>${courses}</strong><small>Active courses · live catalogue</small><div class="bar"><i style="width:${Math.min(100,completion)}%"></i></div><small class="good">${completion}% completion · ${certs} certificate(s)</small>`;
 const dept=document.querySelector('.deptGrid [data-open="finance"]');if(dept){let b=dept.querySelector('b'),s=dept.querySelector('small');if(b)b.textContent=money(tuition)+' approved tuition';if(s)s.textContent=`${approved} approved enrolment(s) · ${money(x.outstanding_tuition)} outstanding`}
 const academic=document.querySelector('.deptGrid [data-open="academic"]');if(academic){let b=academic.querySelector('b'),s=academic.querySelector('small');if(b)b.textContent=courses+' active courses';if(s)s.textContent=certs+' certificate(s)'}
 const stats=document.querySelector('.railWrap .rail:first-child');if(stats){let rows=[...stats.querySelectorAll('.stat')];const set=(label,val)=>{let r=rows.find(z=>z.querySelector('span')?.textContent===label);if(r)r.querySelector('b').textContent=val};set('Total Revenue',money(tuition));set('Approved Tuition Value',money(tuition));set('Total Expenses',money(expenses));set('Net Result',money(net));set('Profit Margin',margin+'%');let rev=rows.find(z=>z.querySelector('span')?.textContent==='Total Revenue');if(rev)rev.querySelector('span').textContent='Approved Tuition Value';let note=stats.querySelector('[data-live-finance-note]');if(!note){note=document.createElement('div');note.dataset.liveFinanceNote='1';note.style.cssText='margin-top:7px;padding-top:7px;border-top:1px solid #edf1f5;font-size:9px;color:#6e7d92;line-height:1.45';stats.appendChild(note)}note.textContent='Canonical live source: courses, student-role profiles, approved enrolments, verified payments, certificates and the Finance cashbook.'}
 const goals=[...document.querySelectorAll('.rail .goal')];const revenueGoal=goals.find(g=>/Revenue Verified/i.test(g.textContent||''));if(revenueGoal){const b=revenueGoal.querySelector('.goalHead b'),bar=revenueGoal.querySelector('.bar i');if(b)b.textContent=verifiedPct+'%';if(bar)bar.style.width=verifiedPct+'%'}
 const systemCheck=[...document.querySelectorAll('.auditRow')].find(r=>/System Check/i.test(r.textContent||''));if(systemCheck){const p=systemCheck.querySelector('p');if(p)p.textContent=`${courses} active course(s) · ${students} student profile(s)`}
}
async function sync(){if(busy||(!dashboardVisible()&&!financeVisible()))return;busy=true;try{const x=await snapshot();if(!x)return;window.FundaFinanceSnapshot=x;window.FundaExecutiveSnapshot=x;if(dashboardVisible())patchExecutive(x);if(financeVisible())patchFinanceCopy(x)}catch(e){console.error('Executive live sync',e)}finally{busy=false}}
function schedule(ms=250){clearTimeout(timer);timer=setTimeout(sync,ms)}
document.addEventListener('click',e=>{const b=e.target.closest?.('#nav button,.nav button');if(!b)return;schedule(350);if(/finance/i.test(b.textContent||'')){setTimeout(sync,900);setTimeout(sync,1600)}if(/dashboard/i.test(b.textContent||'')){setTimeout(sync,800);setTimeout(sync,1500)}},true);
window.addEventListener('focus',sync);document.addEventListener('visibilitychange',()=>{if(!document.hidden)sync()});
setInterval(sync,30000);setTimeout(sync,1400);
})();