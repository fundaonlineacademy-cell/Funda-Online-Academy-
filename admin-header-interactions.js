(()=>{
if(!/admin-v2\.html$/i.test(location.pathname))return;
const $=id=>document.getElementById(id),esc=v=>String(v??'').replace(/[&<>"']/g,m=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[m]));let db;
function css(){if($('ahiCss'))return;let s=document.createElement('style');s.id='ahiCss';s.textContent=`.ahiPanel{display:none;position:fixed;z-index:40000;right:12px;top:76px;width:min(410px,calc(100vw - 24px));max-height:72vh;overflow:auto;background:#fffaf0;color:#142846;border:1px solid #dbc98f;border-radius:16px;box-shadow:0 22px 60px #06142155;padding:14px}.ahiPanel.open{display:block}.ahiHead{display:flex;justify-content:space-between;align-items:start;gap:10px}.ahiPanel h3{margin:0 0 4px;font-size:15px}.ahiPanel p{margin:0 0 10px;font-size:10px;color:#748197}.ahiClose{border:0;background:#142846;color:#fff;border-radius:8px;padding:6px 9px;font-weight:800}.ahiItem{display:grid;grid-template-columns:28px 1fr auto;gap:8px;padding:11px 4px;border-top:1px solid #ece5d8;align-items:start;cursor:pointer}.ahiIcon{width:27px;height:27px;border-radius:50%;display:grid;place-items:center;background:#fff0c9}.ahiItem b{font-size:10px}.ahiItem small{display:block;font-size:9px;color:#6e7d92;margin-top:2px}.ahiTag{font-size:8px;font-weight:900;background:#fff0d6;color:#8a5a05;padding:4px 6px;border-radius:99px}.ahiEmpty{padding:18px 5px;text-align:center;color:#6e7d92;font-size:10px}.safeRoleMobile{display:none;color:#fff;font-size:11px;font-weight:900;white-space:nowrap}.safeMenu{z-index:40001!important}@media(max-width:820px){.safeTop{gap:7px!important}.safeAvatar{width:36px!important;height:36px!important}.safeProfile{gap:5px!important}.safeRoleMobile{display:block}.safeId{display:none!important}.safeProfile>button:last-of-type{font-size:18px;padding:8px 3px!important}.safeMenu{position:fixed!important;right:10px!important;top:76px!important;width:min(300px,calc(100vw - 20px))!important}.ahiPanel{right:8px;top:76px;width:calc(100vw - 16px);box-sizing:border-box;max-height:76vh}}`;document.head.appendChild(s)}
async function getData(){db=db||window.supabase?.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);if(!db)return[];let [en,tk,pay]=await Promise.all([db.from('enrollments').select('id,status,enrollment_status,created_at'),db.from('support_tickets').select('id,subject,status,priority,created_at'),db.from('payments').select('id,status,amount,created_at')]);let items=[];(en.data||[]).filter(x=>['pending','awaiting_review','under_review'].includes(String(x.status||x.enrollment_status||'').toLowerCase())).forEach(x=>items.push({icon:'♙',title:'Enrolment awaiting review',detail:'Student application requires Admissions & Finance review.',tag:'REVIEW',open:'enrolments',date:x.created_at}));(tk.data||[]).filter(x=>!['closed','resolved','solved'].includes(String(x.status||'').toLowerCase())).forEach(x=>items.push({icon:'✉',title:x.subject||'Open support ticket',detail:'Student support request requires attention.',tag:String(x.priority||'OPEN').toUpperCase(),open:'support',date:x.created_at}));(pay.data||[]).filter(x=>String(x.status||'').toLowerCase().includes('reject')||String(x.status||'').toLowerCase().includes('fail')).forEach(x=>items.push({icon:'R',title:'Payment exception',detail:'Failed or rejected payment requires review.',tag:'ACTION',open:'finance',date:x.created_at}));return items.sort((a,b)=>String(b.date||'').localeCompare(String(a.date||'')))}
function close(){ $('ahiPanel')?.classList.remove('open') }
async function refreshCount(){try{let items=await getData(),c=$('safeCount');if(c){c.textContent=Math.min(99,items.length);c.style.display=items.length?'grid':'none'}}catch(e){console.error('Notification count',e)}}
async function bell(e){e?.preventDefault();e?.stopPropagation();let panel=$('ahiPanel');if(!panel){panel=document.createElement('div');panel.id='ahiPanel';panel.className='ahiPanel';document.body.appendChild(panel)}let opening=!panel.classList.contains('open');document.querySelector('.safeMenu')?.classList.remove('open');panel.classList.toggle('open',opening);if(!opening)return;panel.innerHTML='<div class="ahiHead"><div><h3>Notifications</h3><p>Loading Academy items requiring attention…</p></div><button class="ahiClose">Close</button></div>';panel.querySelector('.ahiClose').onclick=close;let items=await getData(),c=$('safeCount');if(c){c.textContent=Math.min(99,items.length);c.style.display=items.length?'grid':'none'}panel.innerHTML=`<div class="ahiHead"><div><h3>Notifications</h3><p>${items.length} outstanding item${items.length===1?'':'s'}. The counter reaches zero when these items are resolved in their departments.</p></div><button class="ahiClose">Close</button></div>${items.length?items.slice(0,30).map(x=>`<div class="ahiItem" data-open="${esc(x.open)}"><span class="ahiIcon">${esc(x.icon)}</span><div><b>${esc(x.title)}</b><small>${esc(x.detail)}</small></div><span class="ahiTag">${esc(x.tag)}</span></div>`).join(''):'<div class="ahiEmpty">All caught up — there are no outstanding Academy notifications.</div>'}`;panel.querySelector('.ahiClose').onclick=close;panel.querySelectorAll('[data-open]').forEach(x=>x.onclick=()=>{let dest=x.dataset.open;close();if(typeof window.show==='function')window.show(dest);else if(typeof show==='function')show(dest)})}
function enhanceProfile(){let p=$('safeProfileBtn');if(!p)return;let profile=p.closest('.safeProfile');if(profile&&!profile.querySelector('.safeRoleMobile')){let role=document.createElement('span');role.className='safeRoleMobile';let desktop=profile.querySelector('.safeId b');role.textContent=desktop?.textContent||'CEO';profile.insertBefore(role,profile.querySelector('#safeChevron')||profile.children[1]||null)}}
function wire(){let b=$('safeBell');if(b&&!b.dataset.ahi){b.dataset.ahi='1';b.onclick=bell;b.setAttribute('aria-label','Open notifications')}enhanceProfile();let profile=$('safeProfileBtn'),chev=$('safeChevron');[profile,chev].filter(Boolean).forEach(x=>{if(x.dataset.ahi)return;x.dataset.ahi='1';x.addEventListener('click',()=>close())})}
function init(){css();wire();setTimeout(refreshCount,1000);new MutationObserver(wire).observe(document.body,{childList:true,subtree:true});document.addEventListener('click',e=>{if(!e.target.closest('#ahiPanel,#safeBell'))close()})}if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',init);else init();
})();