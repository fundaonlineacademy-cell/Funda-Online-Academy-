(()=>{
'use strict';
if(!/admin-v2\.html$/i.test(location.pathname)||window.__fundaCanonicalFinanceCentre)return;
window.__fundaCanonicalFinanceCentre=true;
let db,user,bound=false,busy=false,last=null;
const $=id=>document.getElementById(id),n=v=>Number(v||0),money=v=>'R'+n(v).toLocaleString('en-ZA',{minimumFractionDigits:2,maximumFractionDigits:2});
const css=`.finx{margin-top:12px}.finHero{padding:18px;border-radius:15px;background:linear-gradient(135deg,#03101f,#0a2c52);color:white}.finHero b{color:#d4af58;font-size:10px;letter-spacing:.1em}.finHero h2{margin:4px 0;font-size:20px}.finHero p{margin:0;color:#d9e4f0;font-size:10px}.finLive{margin-top:8px;font-size:9px;color:#bad1e6}.finK{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:9px 0}.finCard,.finPanel{background:#fff;border:1px solid #e0dacb;border-radius:11px;padding:12px}.finCard strong{display:block;font-size:17px;color:#071b31}.finCard span{font-size:9px;color:#718096}.finTools{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.finTool{background:#fff;border:1px solid #ddd5c2;border-radius:11px;padding:13px;cursor:pointer}.finTool:hover{border-color:#d4af58}.finTool h3{font-size:12px;margin:5px 0;color:#10213f}.finTool p{font-size:9px;color:#718096;line-height:1.5}.finBtn{border:0;border-radius:7px;background:#071b31;color:#efd78e;padding:8px 10px;font-size:9px;font-weight:900;cursor:pointer}.finBtn.alt{background:#fff;color:#071b31;border:1px solid #ddd4bd}.finGrid{display:grid;grid-template-columns:1.1fr .9fr;gap:8px;margin-top:8px}.finPanel h3{margin:0 0 8px;font-size:13px;color:#10213f}.finRow{display:flex;justify-content:space-between;gap:12px;border-bottom:1px solid #eee9df;padding:6px 0;font-size:10px}.finRow.total{font-weight:900;font-size:11px}.finWarn{color:#a56608}.finGood{color:#16805f}.finInfo{margin:8px 0;padding:9px 11px;border-radius:8px;background:#f5f8fc;border:1px solid #dce5ef;color:#52657d;font-size:9px;line-height:1.5}.finConfirm{margin:10px 0;padding:9px 11px;border-radius:8px;background:#e9f7ef;border:1px solid #bfe4cf;color:#176b50;font-size:10px;font-weight:800}.finPending{margin:8px 0;padding:9px 11px;border-radius:8px;background:#fff7e7;border:1px solid #f0d9a8;color:#8a5a05;font-size:10px}.finModal{position:fixed;inset:0;background:#020a14b8;z-index:9999;display:grid;place-items:center;padding:15px}.finBox{background:#fff;border-radius:14px;width:min(760px,96vw);max-height:88vh;overflow:auto;padding:18px}.finBox h2{color:#071b31;margin:0 0 5px}.finFields{display:grid;grid-template-columns:1fr 1fr;gap:8px}.finFields input,.finFields select,.finFields textarea{width:100%;box-sizing:border-box;border:1px solid #ddd4bd;border-radius:7px;padding:8px;font-size:10px}.finActions{display:flex;gap:7px;justify-content:flex-end;flex-wrap:wrap;margin-top:12px}.finError{padding:12px;border:1px solid #efcaca;border-radius:10px;background:#fff0f0;color:#982c2c;font-size:10px}@media(max-width:900px){.finK{grid-template-columns:repeat(2,1fr)}.finTools,.finGrid{grid-template-columns:1fr}.finFields{grid-template-columns:1fr}}`;
function style(){if($('finStyle'))return;const s=document.createElement('style');s.id='finStyle';s.textContent=css;document.head.appendChild(s)}
function active(){return [...document.querySelectorAll('#nav button,.nav button')].some(x=>(x.classList.contains('active')||x.classList.contains('on'))&&/finance/i.test(x.textContent||''))}
async function client(){if(db)return db;db=window.supabase?.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);if(!db)return null;const s=await db.auth.getSession();user=s.data.session?.user||null;return db}
async function snapshot(){const c=await client();if(!c||!user)return null;const r=await c.rpc('get_admin_finance_snapshot');if(r.error)throw r.error;last=r.data||{};window.FundaFinanceSnapshot=last;return last}
async function period(from,to){const c=await client();const r=await c.rpc('get_admin_finance_period',{p_from:from||null,p_to:to||null});if(r.error)throw r.error;return r.data||{}}
function renderMarkup(x){const generated=x.generated_at?new Date(x.generated_at).toLocaleString('en-ZA',{dateStyle:'medium',timeStyle:'short'}):'now';return `<div class="finx"><div class="finHero"><b>FINANCE & ACCOUNTING</b><h2>Live Finance Control Centre</h2><p>One financial source of truth for enrolments, verified payments, receivables, cashbook and executive reporting.</p><div class="finLive">LIVE DATABASE SNAPSHOT · ${generated}</div></div><div class="finK"><div class="finCard"><strong>${money(x.approved_tuition)}</strong><span>Approved tuition · agreed enrolment fees</span></div><div class="finCard"><strong>${money(x.verified_payments_all)}</strong><span>Verified collections</span></div><div class="finCard"><strong>${money(x.outstanding_tuition)}</strong><span>Outstanding approved tuition</span></div><div class="finCard"><strong>${money(x.operating_expenses)}</strong><span>Recorded expenses</span></div><div class="finCard"><strong class="${n(x.net_cash_result)>=0?'finGood':'finWarn'}">${money(x.net_cash_result)}</strong><span>Net cash result</span></div></div><div class="finTools"><div class="finTool" id="finIncome"><h3>▥ Management Income Statement</h3><p>Generate a period report directly from the same live finance rules.</p><button class="finBtn">Generate Statement</button></div><div class="finTool" id="finRecon"><h3>✓ Reconciliation Centre</h3><p>Compare an external statement with the system's canonical period cash result.</p><button class="finBtn">Reconcile</button></div><div class="finTool" id="finAcct"><h3>▤ Accountant Pack</h3><p>Prepare an accountant-ready period summary from the canonical finance source.</p><button class="finBtn">Prepare Pack</button></div><div class="finTool" id="finReport"><h3>⇩ Finance Reports</h3><p>Open the Report Centre for detailed Finance & Accounting exports.</p><button class="finBtn">Open Reports</button></div></div><div class="finGrid"><div class="finPanel"><h3>Collections & Receivables</h3><div class="finRow"><span>Approved tuition value</span><b>${money(x.approved_tuition)}</b></div><div class="finRow"><span>Verified against approved enrolments</span><b>${money(x.verified_against_approved)}</b></div><div class="finRow"><span>Submitted / awaiting verification</span><b class="finWarn">${money(x.pending_payment_value)}</b></div><div class="finRow total"><span>Outstanding approved tuition</span><b>${money(x.outstanding_tuition)}</b></div><div class="finInfo">${n(x.approved_students)} approved learner(s) across ${n(x.approved_enrolments)} approved enrolment(s). ${n(x.outstanding_enrolments)} enrolment(s) currently have an outstanding balance.</div></div><div class="finPanel"><h3>Live Finance Controls</h3><div class="finRow"><span>Payment records</span><b>${n(x.payment_records)}</b></div><div class="finRow"><span>Payments awaiting decision</span><b class="${n(x.pending_payment_records)?'finWarn':'finGood'}">${n(x.pending_payment_records)}</b></div><div class="finRow"><span>Cashbook entries</span><b>${n(x.cashbook_entries)}</b></div><div class="finRow"><span>Other recorded income</span><b>${money(x.other_income)}</b></div><div class="finInfo"><b>Price integrity:</b> historical approved learner fees remain fixed at the enrolment amount. Today's catalogue would value the same enrolments at ${money(x.catalogue_reprice_value)} (${money(x.catalogue_variance)} variance), but that variance is not treated as revenue.</div></div></div></div>`}
async function render(){if(!active()||busy)return;busy=true;try{const v=$('view');if(!v)return;const x=await snapshot();if(!x)return;v.innerHTML=renderMarkup(x);$('finIncome').onclick=incomeModal;$('finRecon').onclick=reconModal;$('finAcct').onclick=accountantModal;$('finReport').onclick=()=>window.openFundaReportCentre?window.openFundaReportCentre('Finance & Accounting'):alert('Report Centre is loading. Please try again.')}catch(e){console.error('Finance Control Centre',e);const v=$('view');if(v&&active())v.innerHTML=`<div class="finError"><b>Finance data could not refresh.</b><br>${String(e.message||e)}<br>The rest of the Admin Command Center is unaffected.</div>`}finally{busy=false}}
function dates(){const d=new Date(),f=new Date(d.getFullYear(),d.getMonth(),1);return[f.toISOString().slice(0,10),d.toISOString().slice(0,10)]}
function fmtDate(v){if(!v)return'All dates';return new Date(v+'T12:00:00').toLocaleDateString('en-ZA',{day:'2-digit',month:'short',year:'numeric'})}
function modal(title,body){$('finModal')?.remove();document.body.insertAdjacentHTML('beforeend',`<div class="finModal" id="finModal"><div class="finBox"><h2>${title}</h2>${body}<div class="finActions"><button class="finBtn alt" id="finClose">Close</button></div></div></div>`);$('finClose').onclick=()=>$('finModal')?.remove()}
async function incomeModal(){const [f,t]=dates();modal('Management Income Statement',`<p class="finInfo">Confirmed income is recognised from verified payment records plus non-payment cashbook income. Submitted/unverified payments remain disclosed separately.</p><div class="finFields"><label>From<input type="date" id="ifs" value="${f}"></label><label>To<input type="date" id="its" value="${t}"></label></div><div id="isBody" style="margin-top:8px">Loading…</div><div class="finActions"><button class="finBtn" id="isGen">Generate / Refresh</button><button class="finBtn" id="isPrint">Print / Save PDF</button></div>`);const gen=async()=>{try{const from=$('ifs').value,to=$('its').value;if(from&&to&&from>to)throw new Error('Start date cannot be after end date');const x=await period(from,to);$('isBody').innerHTML=`<div class="finConfirm">✓ Live statement · ${fmtDate(from)} – ${fmtDate(to)}</div><div class="finRow"><span>Verified tuition revenue</span><b>${money(x.verified_tuition)}</b></div><div class="finRow"><span>Other recorded income</span><b>${money(x.other_income)}</b></div><div class="finRow total"><span>Total confirmed income</span><b>${money(x.confirmed_income)}</b></div><div class="finRow"><span>Operating expenses</span><b>(${money(x.expenses)})</b></div><div class="finRow total"><span>Net Surplus / (Loss)</span><b>${money(x.net_result)}</b></div><div class="finPending"><b>Submitted / unverified collections: ${money(x.pending_collections)}</b><br>Excluded from confirmed income until Finance verifies them.</div><div class="finInfo">Records in period: ${n(x.payment_records)} payment record(s), ${n(x.cashbook_entries)} cashbook entry/entries.</div>`}catch(e){$('isBody').innerHTML=`<div class="finError">${String(e.message||e)}</div>`}};$('isGen').onclick=gen;$('isPrint').onclick=()=>window.print();gen()}
async function reconModal(){const [f,t]=dates();modal('Reconciliation Centre',`<p class="finInfo">The system total comes from the same protected period calculation used by Finance reporting.</p><div class="finFields"><input type="date" id="rfs" value="${f}"><input type="date" id="rts" value="${t}"><input id="raccount" placeholder="Account name" value="Primary Account"><input id="rstatement" type="number" step="0.01" placeholder="Statement closing/net total"><textarea id="rnotes" style="grid-column:1/-1" placeholder="Reconciliation notes"></textarea></div><div class="finActions"><button class="finBtn" id="rSave">Save Reconciliation</button></div>`);$('rSave').onclick=async()=>{try{const x=await period($('rfs').value,$('rts').value),statement=n($('rstatement').value),c=await client();const r=await c.from('finance_reconciliations').insert({period_start:$('rfs').value,period_end:$('rts').value,account_name:$('raccount').value||'Primary Account',statement_total:statement,system_total:n(x.net_result),status:Math.abs(statement-n(x.net_result))<0.01?'reconciled':'difference',notes:$('rnotes').value,reconciled_by:user.id,reconciled_at:new Date().toISOString()});if(r.error)throw r.error;alert(`Reconciliation saved. Difference: ${money(statement-n(x.net_result))}`);$('finModal')?.remove();render()}catch(e){alert(e.message||e)}}}
async function accountantModal(){const [f,t]=dates();modal('Accountant / Tax Preparation Pack',`<p class="finInfo">This is a management summary for accountant review. Tax/VAT liabilities are not calculated unless formal tax settings are configured.</p><div class="finFields"><input type="date" id="afs" value="${f}"><input type="date" id="ats" value="${t}"><input id="aname" placeholder="Accountant / bookkeeper name"><input id="aemail" type="email" placeholder="Accountant email"><textarea id="anotes" style="grid-column:1/-1" placeholder="Preparation notes"></textarea></div><div class="finActions"><button class="finBtn" id="aPreview">Preview Summary</button><button class="finBtn" id="aSave">Mark Pack Prepared</button></div><div id="aBody"></div>`);$('aPreview').onclick=async()=>{try{const x=await period($('afs').value,$('ats').value);$('aBody').innerHTML=`<div style="margin-top:12px"><div class="finRow"><span>Total confirmed income</span><b>${money(x.confirmed_income)}</b></div><div class="finRow"><span>Submitted/unverified collections</span><b>${money(x.pending_collections)}</b></div><div class="finRow"><span>Total expenses</span><b>${money(x.expenses)}</b></div><div class="finRow"><span>Net management result</span><b>${money(x.net_result)}</b></div></div>`}catch(e){$('aBody').innerHTML=`<div class="finError">${String(e.message||e)}</div>`}};$('aSave').onclick=async()=>{try{const c=await client(),r=await c.from('finance_accountant_periods').insert({period_start:$('afs').value,period_end:$('ats').value,pack_status:'prepared',accountant_name:$('aname').value,accountant_email:$('aemail').value,notes:$('anotes').value,prepared_by:user.id,prepared_at:new Date().toISOString()});if(r.error)throw r.error;alert('Accountant preparation period recorded.');$('finModal')?.remove()}catch(e){alert(e.message||e)}}}
function bind(){style();if(bound)return true;const nav=[...document.querySelectorAll('#nav button,.nav button')].find(x=>/finance/i.test(x.textContent||''));if(!nav)return false;bound=true;nav.addEventListener('click',()=>setTimeout(render,300));return true}
function refreshIfVisible(){if(active())render()}
let tries=0,t=setInterval(()=>{tries++;if(bind()||tries>30)clearInterval(t)},250);
window.addEventListener('focus',refreshIfVisible);document.addEventListener('visibilitychange',()=>{if(!document.hidden)refreshIfVisible()});setInterval(refreshIfVisible,30000);setTimeout(()=>{bind();refreshIfVisible()},1200);
})();