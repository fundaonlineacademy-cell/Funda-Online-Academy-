(()=>{if(!/admin-v2\.html$/i.test(location.pathname))return;let db,user,P=[],E=[],C=[],R=[],bound=false;const $=id=>document.getElementById(id),money=n=>'R'+Number(n||0).toLocaleString('en-ZA',{minimumFractionDigits:2,maximumFractionDigits:2}),st=x=>String(x||'').toLowerCase(),paid=s=>['verified','paid','approved','completed'].includes(st(s)),css=`.finx{margin-top:12px}.finHero{padding:18px;border-radius:15px;background:linear-gradient(135deg,#03101f,#0a2c52);color:white}.finHero b{color:#d4af58;font-size:10px;letter-spacing:.1em}.finHero h2{margin:4px 0;font-size:20px}.finHero p{margin:0;color:#d9e4f0;font-size:10px}.finK{display:grid;grid-template-columns:repeat(5,1fr);gap:8px;margin:9px 0}.finCard,.finPanel{background:#fff;border:1px solid #e0dacb;border-radius:11px;padding:12px}.finCard strong{display:block;font-size:17px;color:#071b31}.finCard span{font-size:9px;color:#718096}.finTools{display:grid;grid-template-columns:repeat(4,1fr);gap:8px}.finTool{background:#fff;border:1px solid #ddd5c2;border-radius:11px;padding:13px;cursor:pointer}.finTool:hover{border-color:#d4af58}.finTool h3{font-size:12px;margin:5px 0;color:#10213f}.finTool p{font-size:9px;color:#718096;line-height:1.5}.finBtn{border:0;border-radius:7px;background:#071b31;color:#efd78e;padding:8px 10px;font-size:9px;font-weight:900;cursor:pointer}.finGrid{display:grid;grid-template-columns:1.1fr .9fr;gap:8px;margin-top:8px}.finPanel h3{margin:0 0 8px;font-size:13px;color:#10213f}.finRow{display:flex;justify-content:space-between;border-bottom:1px solid #eee9df;padding:6px 0;font-size:10px}.finRow.total{font-weight:900;font-size:11px}.finWarn{color:#a56608}.finGood{color:#16805f}.finConfirm{margin:10px 0;padding:9px 11px;border-radius:8px;background:#e9f7ef;border:1px solid #bfe4cf;color:#176b50;font-size:10px;font-weight:800}.finPending{margin:8px 0;padding:9px 11px;border-radius:8px;background:#fff7e7;border:1px solid #f0d9a8;color:#8a5a05;font-size:10px}.finModal{position:fixed;inset:0;background:#020a14b8;z-index:9999;display:grid;place-items:center;padding:15px}.finBox{background:#fff;border-radius:14px;width:min(760px,96vw);max-height:88vh;overflow:auto;padding:18px}.finBox h2{color:#071b31;margin:0 0 5px}.finFields{display:grid;grid-template-columns:1fr 1fr;gap:8px}.finFields input,.finFields select,.finFields textarea{width:100%;box-sizing:border-box;border:1px solid #ddd4bd;border-radius:7px;padding:8px;font-size:10px}.finActions{display:flex;gap:7px;justify-content:flex-end;margin-top:12px}@media(max-width:900px){.finK{grid-template-columns:repeat(2,1fr)}.finTools,.finGrid{grid-template-columns:1fr}.finFields{grid-template-columns:1fr}}`;function style(){if($('finStyle'))return;let s=document.createElement('style');s.id='finStyle';s.textContent=css;document.head.appendChild(s)}async function fetchData(){db=window.supabase?.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);if(!db)return;let ses=await db.auth.getSession();user=ses.data.session?.user;if(!user)return;let [p,e,c,r]=await Promise.all([db.from('payments').select('*').order('created_at',{ascending:false}),db.from('enrollments').select('*').order('created_at',{ascending:false}),db.from('admin_cashbook').select('*').order('entry_date',{ascending:false}),db.from('finance_reconciliations').select('*').order('created_at',{ascending:false})]);P=p.data||[];E=e.data||[];C=c.data||[];R=r.data||[];bind();let a=[...document.querySelectorAll('#nav button,.nav button')].find(x=>(x.classList.contains('active')||x.classList.contains('on'))&&/finance/i.test(x.textContent));if(a)render()}function calc(from,to){let inside=d=>{if(!d)return true;let z=String(d).slice(0,10);return(!from||z>=from)&&(!to||z<=to)},payments=P.filter(x=>inside(x.verified_at||x.created_at)),cash=C.filter(x=>inside(x.entry_date||x.created_at)),verified=payments.filter(x=>paid(x.status)).reduce((a,x)=>a+Number(x.amount||0),0),submitted=payments.filter(x=>!paid(x.status)).reduce((a,x)=>a+Number(x.amount||0),0),other=cash.filter(x=>st(x.entry_type)==='income').reduce((a,x)=>a+Number(x.amount||0),0),expenses=cash.filter(x=>st(x.entry_type)==='expense').reduce((a,x)=>a+Number(x.amount||0),0),income=verified+other;return{verified,submitted,other,expenses,income,net:income-expenses,payments,cash}}function bind(){style();if(bound)return true;let nav=[...document.querySelectorAll('#nav button,.nav button')].find(x=>/finance/i.test(x.textContent));if(!nav)return false;bound=true;nav.addEventListener('click',()=>setTimeout(render,220));return true}function render(){let v=$('view');if(!v)return;let active=[...document.querySelectorAll('#nav button,.nav button')].find(x=>x.classList.contains('active')||x.classList.contains('on'));if(active&&!/finance/i.test(active.textContent))return;let x=calc(),pipeline=E.filter(e=>['approved','completed'].includes(st(e.status||e.enrollment_status))).reduce((a,e)=>a+Number(e.amount||0),0),out=Math.max(0,pipeline-x.verified);v.innerHTML=`<div class="finx"><div class="finHero"><b>FINANCE & ACCOUNTING</b><h2>Finance Control Centre</h2><p>Collections, cashbook, reconciliation, management reporting and accountant-ready financial controls.</p></div><div class="finK"><div class="finCard"><strong>${money(x.verified)}</strong><span>Verified tuition revenue</span></div><div class="finCard"><strong>${money(pipeline)}</strong><span>Approved tuition pipeline</span></div><div class="finCard"><strong>${money(out)}</strong><span>Outstanding tuition</span></div><div class="finCard"><strong>${money(x.expenses)}</strong><span>Recorded expenses</span></div><div class="finCard"><strong class="${x.net>=0?'finGood':'finWarn'}">${money(x.net)}</strong><span>Net cash result</span></div></div><div class="finTools"><div class="finTool" id="finIncome"><h3>▥ Management Income Statement</h3><p>Generate revenue, other income, expenses and net result for a selected period.</p><button class="finBtn">Generate Statement</button></div><div class="finTool" id="finRecon"><h3>✓ Reconciliation Centre</h3><p>Compare financial records with an external bank/payment statement and record differences.</p><button class="finBtn">Reconcile</button></div><div class="finTool" id="finAcct"><h3>▤ Accountant Pack</h3><p>Prepare a clean period summary for bookkeeping, accountant review and tax preparation.</p><button class="finBtn">Prepare Pack</button></div><div class="finTool" id="finReport"><h3>⇩ Finance Reports</h3><p>Open the existing Report Centre for detailed Finance & Accounting reports and CSV export.</p><button class="finBtn">Open Reports</button></div></div><div class="finGrid"><div class="finPanel"><h3>Collections & Receivables</h3><div class="finRow"><span>Approved tuition value</span><b>${money(pipeline)}</b></div><div class="finRow"><span>Verified collections</span><b>${money(x.verified)}</b></div><div class="finRow"><span>Submitted / awaiting verification</span><b class="finWarn">${money(x.submitted)}</b></div><div class="finRow total"><span>Outstanding against approved tuition</span><b>${money(out)}</b></div></div><div class="finPanel"><h3>Finance Controls</h3><div class="finRow"><span>Payment records</span><b>${P.length}</b></div><div class="finRow"><span>Unverified/submitted payments</span><b class="${P.filter(p=>!paid(p.status)).length?'finWarn':'finGood'}">${P.filter(p=>!paid(p.status)).length}</b></div><div class="finRow"><span>Cashbook entries</span><b>${C.length}</b></div><div class="finRow"><span>Reconciliations recorded</span><b>${R.length}</b></div></div></div></div>`;$('finIncome').onclick=incomeModal;$('finRecon').onclick=reconModal;$('finAcct').onclick=accountantModal;$('finReport').onclick=()=>window.openFundaReportCentre?window.openFundaReportCentre('Finance & Accounting'):alert('Report Centre is loading. Please try again.')}function dates(){let n=new Date(),f=new Date(n.getFullYear(),n.getMonth(),1);return[f.toISOString().slice(0,10),n.toISOString().slice(0,10)]}function modal(title,body){$('finModal')?.remove();document.body.insertAdjacentHTML('beforeend',`<div class="finModal" id="finModal"><div class="finBox"><h2>${title}</h2>${body}<div class="finActions"><button class="finBtn" id="finClose">Close</button></div></div></div>`);$('finClose').onclick=()=>$('finModal')?.remove()}function fmtDate(v){if(!v)return'—';return new Date(v+'T12:00:00').toLocaleDateString('en-ZA',{day:'2-digit',month:'short',year:'numeric'})}function incomeModal(){let [f,t]=dates();modal('Management Income Statement',`<p style="font-size:10px;color:#718096">Operational management report based on confirmed financial records. Submitted/unverified collections are disclosed separately and are not recognised as confirmed revenue.</p><div class="finFields"><label style="font-size:9px">From<input type="date" id="ifs" value="${f}"></label><label style="font-size:9px">To<input type="date" id="its" value="${t}"></label></div><div id="isStatus"></div><div id="isBody" style="margin-top:8px"></div><div class="finActions"><button class="finBtn" id="isGen">Generate / Refresh</button><button class="finBtn" id="isPrint">Print / Save PDF</button></div>`);let gen=()=>{let from=$('ifs').value,to=$('its').value;if(from&&to&&from>to){$('isStatus').innerHTML='<div class="finPending">Start date cannot be after end date.</div>';return}let x=calc(from,to),now=new Date().toLocaleTimeString('en-ZA',{hour:'2-digit',minute:'2-digit',second:'2-digit'});$('isStatus').innerHTML=`<div class="finConfirm">✓ Statement generated for ${fmtDate(from)} – ${fmtDate(to)} · Generated at ${now}</div>`;$('isBody').innerHTML=`<div class="finRow"><span>Verified tuition revenue</span><b>${money(x.verified)}</b></div><div class="finRow"><span>Other recorded income</span><b>${money(x.other)}</b></div><div class="finRow total"><span>Total confirmed income</span><b>${money(x.income)}</b></div><div class="finRow"><span>Operating expenses</span><b>(${money(x.expenses)})</b></div><div class="finRow total"><span>Net Surplus / (Loss)</span><b>${money(x.net)}</b></div><div class="finPending"><b>Submitted / unverified collections: ${money(x.submitted)}</b><br>This amount is visible for control purposes but is excluded from confirmed revenue until Finance verifies it.</div><div style="font-size:9px;color:#718096;margin-top:7px">Records reviewed: ${x.payments.length} payment record(s) and ${x.cash.length} cashbook entry/entries in this period.</div>`};$('isGen').onclick=gen;$('isPrint').onclick=()=>window.print();gen()}function reconModal(){let [f,t]=dates();modal('Reconciliation Centre',`<p style="font-size:10px;color:#718096">Enter the external statement total for the period. The system total is based on verified tuition plus cashbook income less expenses.</p><div class="finFields"><input type="date" id="rfs" value="${f}"><input type="date" id="rts" value="${t}"><input id="raccount" placeholder="Account name" value="Primary Account"><input id="rstatement" type="number" step="0.01" placeholder="Statement closing/net total"><textarea id="rnotes" style="grid-column:1/-1" placeholder="Reconciliation notes"></textarea></div><div class="finActions"><button class="finBtn" id="rSave">Save Reconciliation</button></div>`);$('rSave').onclick=async()=>{let z=calc($('rfs').value,$('rts').value),statement=Number($('rstatement').value||0),{error}=await db.from('finance_reconciliations').insert({period_start:$('rfs').value,period_end:$('rts').value,account_name:$('raccount').value||'Primary Account',statement_total:statement,system_total:z.net,status:Math.abs(statement-z.net)<0.01?'reconciled':'difference',notes:$('rnotes').value,reconciled_by:user.id,reconciled_at:new Date().toISOString()});if(error)return alert(error.message);alert(`Reconciliation saved. Difference: ${money(statement-z.net)}`);$('finModal').remove();fetchData()}}function accountantModal(){let [f,t]=dates();modal('Accountant / Tax Preparation Pack',`<p style="font-size:10px;color:#718096">Creates a management summary for accountant review. Tax/VAT liability is not calculated unless the Academy's tax registration and rules are formally configured.</p><div class="finFields"><input type="date" id="afs" value="${f}"><input type="date" id="ats" value="${t}"><input id="aname" placeholder="Accountant / bookkeeper name"><input id="aemail" type="email" placeholder="Accountant email"><textarea id="anotes" style="grid-column:1/-1" placeholder="Preparation notes"></textarea></div><div class="finActions"><button class="finBtn" id="aPreview">Preview Summary</button><button class="finBtn" id="aSave">Mark Pack Prepared</button></div><div id="aBody"></div>`);$('aPreview').onclick=()=>{let x=calc($('afs').value,$('ats').value);$('aBody').innerHTML=`<div style="margin-top:12px"><div class="finRow"><span>Total confirmed income</span><b>${money(x.income)}</b></div><div class="finRow"><span>Submitted/unverified collections</span><b>${money(x.submitted)}</b></div><div class="finRow"><span>Total expenses</span><b>${money(x.expenses)}</b></div><div class="finRow"><span>Net management result</span><b>${money(x.net)}</b></div><div class="finRow"><span>Payment records in period</span><b>${x.payments.length}</b></div><div class="finRow"><span>Cashbook records in period</span><b>${x.cash.length}</b></div></div>`};$('aSave').onclick=async()=>{let {error}=await db.from('finance_accountant_periods').insert({period_start:$('afs').value,period_end:$('ats').value,pack_status:'prepared',accountant_name:$('aname').value,accountant_email:$('aemail').value,notes:$('anotes').value,prepared_by:user.id,prepared_at:new Date().toISOString()});if(error)return alert(error.message);alert('Accountant preparation period recorded.');$('finModal').remove()}}style();let tries=0,t=setInterval(()=>{tries++;if(bind()||tries>=20)clearInterval(t)},250);setTimeout(fetchData,700)})();