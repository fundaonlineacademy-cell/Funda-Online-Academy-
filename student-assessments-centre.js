// FUNDA ONLINE ACADEMY — DEDICATED STUDENT ASSESSMENTS CENTRE
(function(){
'use strict';
if(!/dashboard\.html$/i.test(location.pathname))return;
let db=null,state={user:null,enrolments:[],courses:[],modules:[],assessments:[],progress:[],attempts:[]};
const esc=v=>String(v??'').replace(/[&<>'"]/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;',"'":'&#39;','"':'&quot;'}[ch]));
const approved=e=>['approved','active','enrolled','completed'].includes(String(e.enrollment_status||e.status||'').toLowerCase());
const typeOf=a=>/summative/i.test(a.title||'')?'summative':/formative/i.test(a.title||'')?'formative':'assessment';
function css(){if(document.getElementById('studentAssessmentsCss'))return;const s=document.createElement('style');s.id='studentAssessmentsCss';s.textContent=`
#studentAssessmentsCentre{max-width:1180px;margin:22px auto;padding:0 18px}.saHead{background:linear-gradient(135deg,#fffdf7,#fff5dc);border:1px solid #ead8a6;border-radius:22px;padding:22px}.saK{font-size:9px;letter-spacing:.18em;font-weight:900;color:#b58216}.saHead h2{margin:6px 0;color:#17324a;font:900 24px Montserrat,sans-serif}.saHead p{margin:0;color:#657383;font-size:12px;line-height:1.65}.saCourse{margin-top:18px;background:#fff;border:1px solid #dfe5ea;border-radius:20px;overflow:hidden}.saCourseHead{padding:17px 19px;background:#f8fafb;border-bottom:1px solid #e4e8ec}.saCourseHead h3{margin:0;color:#17324a;font:900 15px Montserrat,sans-serif}.saModule{padding:17px 19px;border-bottom:1px solid #edf0f2}.saModuleTitle{display:flex;justify-content:space-between;gap:12px;margin-bottom:12px}.saModuleTitle b{color:#17324a;font-size:13px}.saModuleTitle span{font-size:10px;color:#6f7c89}.saGrid{display:grid;grid-template-columns:1fr 1fr;gap:10px}.saCard{border:1px solid #e0e5e9;border-radius:14px;padding:13px}.saCard.locked{background:#f8f9fa}.saCard h4{margin:0 0 5px;color:#17324a;font-size:12px}.saCard p{margin:8px 0;color:#697684;font-size:10px;line-height:1.5}.saBadge{display:inline-block;border-radius:999px;padding:5px 8px;font-size:8px;font-weight:900;background:#eef2f5;color:#566573}.saBadge.ready{background:#e8f6ed;color:#21663b}.saBadge.done{background:#fff1c9;color:#79550b}.saBtn{display:inline-flex;margin-top:4px;padding:9px 11px;border-radius:10px;background:#17324a;color:#fff!important;text-decoration:none;font-size:9px;font-weight:900}.saBtn.disabled{background:#d9dee2;color:#7c8790!important;pointer-events:none}.saEmpty{padding:24px;text-align:center;color:#687684;font-size:12px}@media(max-width:700px){.saGrid{grid-template-columns:1fr}.saModuleTitle{display:block}.saModuleTitle span{display:block;margin-top:5px}}`;document.head.appendChild(s)}
function mount(){let r=document.getElementById('studentAssessmentsCentre');if(!r){r=document.createElement('section');r.id='studentAssessmentsCentre';r.hidden=true;(document.getElementById('dashboardContent')||document.body).appendChild(r)}return r}
async function load(){if(!window.supabase)return;db=window.supabase.createClient(window.SUPABASE_URL,window.SUPABASE_ANON_KEY);const {data:{user}}=await db.auth.getUser();if(!user)return;state.user=user;const {data:ens}=await db.from('enrollments').select('id,course_id,enrollment_status,status').eq('student_id',user.id);state.enrolments=(ens||[]).filter(approved);const ids=[...new Set(state.enrolments.map(e=>e.course_id).filter(Boolean))];if(!ids.length){render();return}const rs=await Promise.all([db.from('courses').select('id,title').in('id',ids),db.from('course_modules').select('id,course_id,module_number,module_name').in('course_id',ids).order('module_number'),db.from('assessments').select('id,course_id,module_id,title').in('course_id',ids).eq('active',true).eq('status','published'),db.from('module_progress').select('module_id,completed,completed_at').eq('student_id',user.id),db.from('assessment_attempts').select('assessment_id,attempt_number,percentage,passed,submitted_at').eq('student_id',user.id)]);state.courses=rs[0].data||[];state.modules=rs[1].data||[];state.assessments=rs[2].data||[];state.progress=rs[3].data||[];state.attempts=rs[4].data||[];render()}
const moduleDone=id=>state.progress.some(p=>String(p.module_id)===String(id)&&p.completed===true);
function aState(a,done){const ats=state.attempts.filter(x=>String(x.assessment_id)===String(a.id)),passed=ats.some(x=>x.passed===true);if(passed)return{label:'Passed',cls:'done',msg:'Completed successfully. Your passing result is recorded.',open:true};if(!done)return{label:'Locked',cls:'',msg:'Complete all lessons in this module to unlock this assessment.',open:false};if(typeOf(a)==='summative'){const f=state.assessments.find(x=>String(x.module_id)===String(a.module_id)&&typeOf(x)==='formative');if(f&&!state.attempts.some(x=>String(x.assessment_id)===String(f.id)&&x.passed===true))return{label:'Locked',cls:'',msg:'Pass the module formative assessment first.',open:false}}if(ats.length>=3)return{label:'Finalised',cls:'done',msg:'All permitted attempts have been used.',open:false};return{label:ats.length?'Ready to retry':'Ready',cls:'ready',msg:ats.length?'Review the module and continue when ready.':'Module completed. You may start this assessment.',open:true}}
function card(a,m,done){const st=aState(a,done),type=typeOf(a),href=`module-assessment.html?course=${encodeURIComponent(m.course_id)}&module=${m.module_number}&type=${type}`;return `<article class="saCard ${st.open?'':'locked'}"><h4>${type==='formative'?'Formative Assessment':'Summative Assessment'}</h4><span class="saBadge ${st.cls}">${esc(st.label)}</span><p>${esc(st.msg)}</p><a class="saBtn ${st.open?'':'disabled'}" ${st.open?`href="${href}"`:'aria-disabled="true"'}>${st.open?'Open Assessment':'🔒 Locked'}</a></article>`}
function render(){const root=mount();let h=`<div class="saHead"><div class="saK">STUDENT ASSESSMENTS</div><h2>My Assessments</h2><p>Assessments are organised by course and module. Complete the module first to unlock its assessment. Where both types apply, pass the formative assessment before the summative assessment opens.</p></div>`;if(!state.courses.length)h+=`<div class="saCourse"><div class="saEmpty">No assessments are available yet. They will appear after your course enrolment is approved.</div></div>`;state.courses.forEach(c=>{h+=`<div class="saCourse"><div class="saCourseHead"><h3>${esc(c.title)}</h3></div>`;state.modules.filter(m=>String(m.course_id)===String(c.id)).forEach(m=>{const list=state.assessments.filter(a=>String(a.module_id)===String(m.id));if(!list.length)return;const done=moduleDone(m.id);list.sort((a,b)=>typeOf(a)==='formative'?-1:typeOf(b)==='formative'?1:0);h+=`<div class="saModule"><div class="saModuleTitle"><b>Module ${m.module_number} · ${esc(m.module_name)}</b><span>${done?'Module complete':'Module in progress · assessments locked'}</span></div><div class="saGrid">${list.map(a=>card(a,m,done)).join('')}</div></div>`});h+='</div>'});root.innerHTML=h}
function show(){const dc=document.getElementById('dashboardContent');if(dc)[...dc.children].forEach(x=>{if(x.id!=='studentAssessmentsCentre')x.style.display='none'});const r=mount();r.hidden=false;r.style.display='block';document.querySelectorAll('#sdSide [data-sd-key]').forEach(x=>x.classList.toggle('active',x.dataset.sdKey==='assessments'));window.scrollTo({top:0,behavior:'smooth'})}
function wire(){const a=document.querySelector('#sdSide [data-sd-key="assessments"]');if(!a)return false;if(a.dataset.assessmentCentreWired)return true;a.dataset.assessmentCentreWired='1';a.addEventListener('click',e=>{e.preventDefault();e.stopImmediatePropagation();show()},{capture:true});return true}
function boot(){css();mount();load();let n=0,t=setInterval(()=>{n++;if(wire()||n>30)clearInterval(t)},300)}
if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})();